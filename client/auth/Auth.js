var React = require('react/addons');

var Auth = React.createClass({
    render: function() {
            return (
                <div>
                    <h2>Auth</h2>
                </div>
            );
        }
});

module.exports = Auth;
