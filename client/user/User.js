var React = require('react');

var User = React.createClass({
    render: function() {
            return (
                <div>
                    <h2>User</h2>
                </div>
            );
        }
});

module.exports = User;
