/*** @jsx React.DOM */

var React = require('react');

var SingleGroup = React.createClass({
    render: function() {
            return (
                <div>
                    <h2>SingleGroup</h2>
                </div>
            );
        }
});

module.exports = SingleGroup;
