const React = require('react');

const Forbidden = React.createClass({
    render: function() {
            return (
                <div>
                    <h2>Forbidden</h2>
                </div>
            );
        }
});

module.exports = Forbidden;
